-- Create Helio database (run this first)
-- Connect to PostgreSQL as superuser (postgres) and run this command

CREATE DATABASE helio;

-- Switch to helio database
\c helio;

-- Create the schema by running database_setup.sql